# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        repository: JimmyTournemaine/ha-core
        path: ${{ github.repository }}/ha-core
    - uses: actions/checkout@v4
      with:
        path: ${{ github.repository }}/ha-knocki
    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"
    - name: Run HA setup script
      run: script/setup
      working-directory: ./ha-core
    - name: Merge ha-knocki in core
      run: |
        ln -s "${{ github.repository }}/ha-knocki/knocki" "${{ github.repository }}/ha-core/homeassistant/components/knocki"
        ln -s "${{ github.repository }}/ha-knocki/tests" "${{ github.repository }}/ha-core/tests/components/knocki"
      # ln -s "${{ github.repository }}/ha-knocki/blueprints/automation" "${{ github.repository }}/ha-core/config/blueprints/automation/knocki"
    
